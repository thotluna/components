import React from 'react'

import { Color, Spacing, FontFamily /*, FontSize*/ } from './component'

const Token = () => <></>

Token.Color = Color
Token.Spacing = Spacing
Token.FontFamily = FontFamily
// Token.FontSize = FontSize

export default Token
